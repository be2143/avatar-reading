/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["drive.google.com"], // Add Google Drive or any other external domains here
    },
  };
  
  export default nextConfig;

  